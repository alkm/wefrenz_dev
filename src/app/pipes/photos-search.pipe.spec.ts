import { PhotosSearchPipe } from './photos-search.pipe';

describe('PhotosSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new PhotosSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
