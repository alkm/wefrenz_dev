import { AudioSearchPipe } from './audio-search.pipe';

describe('AudioSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new AudioSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
