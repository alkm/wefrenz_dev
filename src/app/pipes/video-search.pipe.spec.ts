import { VideoSearchPipe } from './video-search.pipe';

describe('VideoSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new VideoSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
